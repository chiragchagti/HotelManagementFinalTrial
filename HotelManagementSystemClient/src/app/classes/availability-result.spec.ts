import { AvailabilityResult } from './availability-result';

describe('AvailabilityResult', () => {
  it('should create an instance', () => {
    expect(new AvailabilityResult()).toBeTruthy();
  });
});
